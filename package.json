{
  "name": "email-dispatch",
  "version": "2.0.0",
  "description": "Email scheduling relay (Resend HTTP API) — Render web service backing a GitHub Pages frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "node-cron": "^3.0.3"
  }
}
